@page "/{clusterIdx:int}/topics/{topicName}/consumers"

@inherits ClusterIndexAwarePage
@rendermode InteractiveServer

<Kafkaf.Components.Topic.TopicComponent ClusterIdx="@clusterIdx"
										PageTitle="page title consumers"
										TopicName="@topicName" CurrentPathPart="consumers" />

<h3>this is topic consumers</h3>

@code {
	[Parameter]
	public string topicName { get; set; } = string.Empty;
}

@page "/{clusterIdx:int}/topics/{topicName}/overview"

@inherits ClusterIndexAwarePage
@rendermode InteractiveServer

<Kafkaf.Components.Topic.TopicComponent ClusterIdx="@clusterIdx"
										PageTitle="page title"
										TopicName="@topicName" CurrentPathPart="overview" />

<h3>this is topic overview</h3>

@code {
	[Parameter]
	public string topicName { get; set; } = string.Empty;
}

@page "/schema-registry"
@rendermode InteractiveServer

<PageTitle>Schema Registry: List</PageTitle>

<h3>Schema Registry: List</h3>

@code {
	protected override Task OnInitializedAsync()
	{
		return base.OnInitializedAsync();
	}
}
